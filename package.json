{
  "name": "hendy-vietsub-studio-pro",
  "version": "2.0.0",
  "scripts": {
    "build": "node build.js",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
